name: Generate Scan Progress Report

on:
  # Re-generate progress frequently so published coverage stays in step with scans.
  schedule:
    - cron: "15 */2 * * *"

  # Allow manual trigger at any time.
  workflow_dispatch:

  # Refresh progress right after scan workflows complete on main.
  workflow_run:
    workflows:
      - URL Validation - Batched
      - Scan Social Media Links
      - Scan Technology Stack
      - Scan Lighthouse
      - Scan Accessibility Statements
    types:
      - completed

# Only one report-generation job at a time; newer runs cancel older queued ones.
concurrency:
  group: scan-progress-report
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  generate-progress-report:
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # Each scan workflow owns one metadata artifact and writes only that one,
      # so concurrent runs can no longer overwrite each other's results.  Collect
      # every artifact and merge them into the working database, which the
      # cross-scanner skip logic reads.  See src/lib/metadata_merge.py.
      - name: Collect metadata artifacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: scripts/fetch-metadata-artifacts.sh data/artifacts

      - name: Merge metadata artifacts
        run: |
          python3 -m src.cli.metadata_artifact merge \
            --into data/metadata.db \
            --from-dir data/artifacts

      - name: Find latest Lighthouse metadata artifact run ID
        id: find-lighthouse-metadata
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          RUN_ID=$(gh api "/repos/${{ github.repository }}/actions/artifacts?name=lighthouse-metadata&per_page=1" \
            --jq '.artifacts[] | select(.expired == false) | .workflow_run.id' 2>/dev/null || echo "")
          echo "run_id=${RUN_ID}" >> $GITHUB_OUTPUT
          if [ -n "$RUN_ID" ]; then
            echo "Found lighthouse-metadata artifact from run $RUN_ID"
          else
            echo "No lighthouse-metadata artifact found — Lighthouse report will fall back to shared metadata DB"
          fi

      - name: Pull latest main before generating reports
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Generate scan progress report
        run: |
          set -o pipefail
          python3 -m src.cli.generate_scan_progress \
            --output docs/scan-progress.md \
            --data docs/scan-progress-data.json \
            --update-index docs/index.md \
            --seeds-dir data/toon-seeds/countries \
            2>&1 | tee scan-progress-output.txt

      - name: Generate social media stats page
        run: |
          set -o pipefail
          python3 -m src.cli.generate_social_media_report \
            --page docs/social-media.md \
            --data docs/social-media-data.json \
            --seeds-dir data/toon-seeds/countries \
            2>&1 | tee social-media-report-output.txt

      - name: Generate domains report
        run: |
          set -o pipefail
          python3 -m src.cli.generate_domains_report \
            --output docs/domains.md \
            2>&1 | tee domains-report-output.txt

      - name: Generate government domain manifest
        run: |
          set -o pipefail
          python3 -m src.cli.generate_gov_domains_manifest \
            --output docs/data/gov-domains.json \
            2>&1 | tee gov-domains-manifest-output.txt

      - name: Generate technology stats page
        run: |
          set -o pipefail
          python3 -m src.cli.generate_technology_report \
            --page docs/technology-scanning.md \
            --data docs/technology-data.json \
            --index docs/technology-index.json \
            --seeds-dir data/toon-seeds/countries \
            2>&1 | tee technology-report-output.txt

      - name: Generate third-party JavaScript stats page
        run: |
          set -o pipefail
          python3 -m src.cli.generate_third_party_js_report \
            --page docs/third-party-tools.md \
            --data docs/third-party-tools-data.json \
            --seeds-dir data/toon-seeds/countries \
            2>&1 | tee third-party-js-report-output.txt

      - name: Generate accessibility statement stats page
        run: |
          set -o pipefail
          python3 -m src.cli.generate_accessibility_report \
            --page docs/accessibility-statements.md \
            --data docs/accessibility-data.json \
            --seeds-dir data/toon-seeds/countries \
            2>&1 | tee accessibility-report-output.txt

      - name: Generate Lighthouse results page
        run: |
          set -o pipefail
          LIGHTHOUSE_DB="data/lighthouse/metadata.db"
          if [ ! -f "$LIGHTHOUSE_DB" ]; then
            LIGHTHOUSE_DB="data/metadata.db"
            echo "No Lighthouse-specific metadata DB found; falling back to shared metadata DB: $LIGHTHOUSE_DB"
          else
            echo "Using Lighthouse-specific metadata DB: $LIGHTHOUSE_DB"
          fi
          python3 -m src.cli.generate_lighthouse_report \
            --page docs/lighthouse-results.md \
            --data docs/lighthouse-data.json \
            --csv docs/lighthouse-data.csv \
            --db "$LIGHTHOUSE_DB" \
            --seeds-dir data/toon-seeds/countries \
            2>&1 | tee lighthouse-report-output.txt

      - name: Generate scan cycle pace report
        run: |
          set -o pipefail
          # Lighthouse keeps its own metadata artifact (data/lighthouse/
          # metadata.db) separate from the shared validation-metadata DB
          # used by every other scanner, so it's evaluated separately and
          # the two tables are combined into one report. See
          # WORKFLOW_ORCHESTRATION_AUDIT.md Section 11 (pulled forward from
          # Phase 5) for the on-pace methodology. --table-only avoids any
          # shell-side text surgery (e.g. tail/head line-trimming, which
          # isn't portable -- BSD head doesn't support negative counts).
          python3 -m src.cli.generate_pace_report \
            --db data/metadata.db \
            --seeds-dir data/toon-seeds/countries \
            --scanner accessibility --scanner social_media \
            --scanner technology --scanner third_party_js --scanner overlays \
            --format markdown --table-only \
            --output /tmp/pace-table-main.md \
            2>&1 | tee pace-report-output.txt

          LIGHTHOUSE_DB="data/lighthouse/metadata.db"
          if [ ! -f "$LIGHTHOUSE_DB" ]; then
            LIGHTHOUSE_DB="data/metadata.db"
          fi
          python3 -m src.cli.generate_pace_report \
            --db "$LIGHTHOUSE_DB" \
            --seeds-dir data/toon-seeds/countries \
            --scanner lighthouse \
            --format markdown --table-only \
            --output /tmp/pace-table-lighthouse.md \
            2>&1 | tee -a pace-report-output.txt

          {
            echo "---"
            echo "title: Scanner Cycle Pace Report"
            echo "layout: page"
            echo "---"
            echo ""
            echo "_Generated: $(date -u +'%Y-%m-%d %H:%M UTC')_"
            echo ""
            echo "Whether each scanner is on pace to complete its target cycle (30 days for most"
            echo "scanners, 60 days for Lighthouse and Relationships), based on distinct URLs scanned"
            echo "in the last 7 days projected forward against the full eligible corpus. See"
            echo "[WORKFLOW_ORCHESTRATION_AUDIT.md](https://github.com/${{ github.repository }}/blob/main/WORKFLOW_ORCHESTRATION_AUDIT.md)"
            echo "Section 11 for the methodology."
            echo ""
            cat /tmp/pace-table-main.md
            # Lighthouse's table has its own header row; drop it so the two
            # tables render as one continuous table under a single header.
            tail -n +3 /tmp/pace-table-lighthouse.md
            echo ""
            echo "_Projection method: \`effective daily throughput = distinct URLs scanned in the last 7 days ÷ 7\`;"
            echo "\`projected cycle days = eligible URLs ÷ effective daily throughput\`. The 7-day measurement"
            echo "window reflects recent scan velocity and is independent of each scanner's own target cycle"
            echo "length -- see src/services/cycle_pace_tracker.py's module docstring for why. 'No data' means"
            echo "the scanner has no rows in its metadata.db within the window (never run against this database,"
            echo "or the database doesn't cover this scanner)._"
          } > docs/pace-report.md

      - name: Commit progress report to repository
        run: |
          # docs/social-media-data.json, docs/accessibility-data.json,
          # docs/technology-data.json, and docs/lighthouse-data.json are excluded
          # from git because they can exceed GitHub's 100 MB file-size limit.
          # All four files are still uploaded as workflow artifacts above.
          # docs/technology-index.json is a compact cross-reference index that
          # IS committed so it can be served via GitHub Pages as a static API.
          # docs/data/gov-domains.json is the same kind of static-API file --
          # network.html fetches it to check government-domain membership.
          git add docs/scan-progress.md docs/domains.md docs/domains/ docs/index.md \
                  docs/social-media.md \
                  docs/technology-scanning.md docs/technology-index.json \
                  docs/third-party-tools.md docs/third-party-tools-data.json \
                  docs/accessibility-statements.md \
                  docs/lighthouse-results.md \
                  docs/data/gov-domains.json \
                  docs/pace-report.md
          if git diff --staged --quiet; then
            echo "No changes to commit — reports are up to date."
          else
            git commit -m "docs: update scan reports and generated site pages [skip ci]"
            git push
            echo "Reports committed and pushed."
          fi

      - name: Upload progress report artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: scan-progress-report-${{ github.run_number }}
          path: |
            docs/scan-progress.md
            docs/scan-progress-data.json
            docs/domains.md
            docs/domains/
            docs/index.md
            docs/social-media.md
            docs/social-media-data.json
            docs/technology-scanning.md
            docs/technology-data.json
            docs/technology-index.json
            docs/third-party-tools.md
            docs/third-party-tools-data.json
            docs/accessibility-statements.md
            docs/accessibility-data.json
            docs/lighthouse-results.md
            docs/lighthouse-data.json
            docs/lighthouse-data.csv
            scan-progress-output.txt
            social-media-report-output.txt
            domains-report-output.txt
            technology-report-output.txt
            third-party-js-report-output.txt
            accessibility-report-output.txt
            lighthouse-report-output.txt
            docs/pace-report.md
            pace-report-output.txt
          retention-days: 30
          if-no-files-found: warn
          compression-level: 0

      - name: Display progress summary
        if: always()
        run: |
          echo "## Scan Progress Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f docs/scan-progress.md ]; then
            cat docs/scan-progress.md >> $GITHUB_STEP_SUMMARY
          else
            echo "_No progress data available yet._" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f docs/pace-report.md ]; then
            tail -n +5 docs/pace-report.md >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Workflow Run: [\#${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
